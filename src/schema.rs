diesel::table! {
    my_table (id) {
        id -> Varchar,
        uid -> Varchar,
        adjdate -> Varchar,
        adjtype -> Varchar,
        remitter -> Varchar,
        beneficiery -> Varchar,
        response -> Varchar,
        txndate -> Varchar,
        txntime -> Varchar,
        rrn -> Varchar,
        terminalid -> Varchar,
        ben_mobile_no -> Varchar,
        rem_mobile_no -> Varchar,
        chbdate -> Varchar,
        chbref -> Varchar,
        txnamount -> Varchar,
        adjamount -> Varchar,
        rem_payee_psp_fee -> Varchar,
        ben_fee -> Varchar,
        ben_fee_sw -> Varchar,
        adjfee -> Varchar,
        npcifee -> Varchar,
        remfeetax -> Varchar,
        benfeetax -> Varchar,
        npcitax -> Varchar,
        adjref -> Varchar,
        bankadjref -> Varchar,
        adjproof -> Varchar,
        compensation_amount -> Varchar,
        adjustment_raised_time -> Varchar,
        no_of_days_for_penalty -> Varchar,
        shdt73 -> Varchar,
        shdt74 -> Varchar,
        shdt75 -> Varchar,
        shdt76 -> Varchar,
        shdt77 -> Varchar,
        transaction_type -> Varchar,
        transaction_indicator -> Varchar,
        beneficiary_account_number -> Varchar,
        remitter_account_number -> Varchar,
        aadhar_number -> Varchar,
        mobile_number -> Varchar,
        payer_psp -> Varchar,
        payee_psp -> Varchar,
        upi_transaction_id -> Varchar,
        virtual_address -> Varchar,
        dispute_flag -> Varchar,
        reason_code -> Varchar,
        mcc -> Varchar,
        originating_channel -> Varchar
    }
}